import { colors } from 'styles/colors'
const pins = {
  startPins: 3,
  pinSize: 1.5,
  pinGap: 20
}

const ball = {
  ballSize: 5.1
}

const engine = {
  engineGravity: 1.0
}

const world = {
  width: 410,
  height: 410
}

export const config = {
  pins,
  ball,
  engine,
  world,
  colors
}
